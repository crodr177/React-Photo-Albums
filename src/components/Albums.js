import React from 'react'
import AlbumsContainer from './AlbumsContainer';

const Albums = (props) => (
  <div id="home-page">
    <h1 className="header">Travel Albums</h1>
    <AlbumsContainer/>
  </div>
)

export default Albums